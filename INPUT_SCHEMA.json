{
  "title": "Real Estate Scraper - Input schema",
  "type": "object",
  "properties": {
    "sources": {
      "title": "Predefined site presets",
      "description": "Choose one or more site presets (select). Use 'other' to provide custom startUrls.",
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "jiji",
          "buyrentkenya",
          "other"
        ]
      },
      "default": ["jiji"],
      "editor": "select"
    },
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "Array of listing page URLs to start crawling from. If you selected a preset in 'sources', you can leave this empty.",
      "items": { "type": "string" }
    },
    "maxRequests": {
      "title": "Maximum number of requests",
      "type": "integer",
      "default": 200
    },
    "waitBetweenRequestsMillis": {
      "title": "Delay between requests (ms)",
      "type": "integer",
      "default": 1000
    },
    "listingLinkSelector": {
      "title": "Listing page link selector",
      "type": "string",
      "default": "a.listing-link"
    },
    "detailSelectors": {
      "title": "Detail page selectors",
      "type": "object",
      "description": "Mapping of fieldName -> CSS selector to extract from detail pages",
      "additionalProperties": { "type": "string" }
    }
  },
  "required": ["startUrls"]
}
